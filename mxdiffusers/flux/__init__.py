"""FLUX.2-klein model implementation (native MLX).

Submodules import mlx and are imported on demand (mlx loads only when a piece is used).
Import the pieces directly, e.g.::

    from mxdiffusers.flux.transformer import Flux2Transformer
"""
