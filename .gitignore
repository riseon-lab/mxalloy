.DS_Store
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/
/outputs/
/models/
/checkpoints/
*.safetensors

# generated experiment artifacts (keep the .py scripts, drop the outputs)
experiments/*.png
experiments/*.log
experiments/*.json

# experimental compiled attention extension (research/, frozen): keep sources, drop build outputs
research/attention_kernel/build/
research/attention_kernel/*.metallib
research/attention_kernel/_ext*.so
# a locally-built artifact may be dropped next to the live module to A/B it
mxalloy/attention/_ext*.so
mxalloy/attention/*.metallib

