"""FLUX.2-klein model implementation (native MLX).

Submodules import mlx and are imported on demand, so ``import mxalloy.models`` stays
free of the mlx dependency. Import the pieces directly, e.g.::

    from mxalloy.models.flux2.transformer import Flux2Transformer
"""
